# MOLMS Runtime Duplicate Audit — 7 September 2026

## `invNewInvoice` — 2 declarations
- line 13135 · hash `9e3f6ac24964` · `function invNewInvoice(){ _invActiveId=null; _invBankManuallyChosen=false; _invLoadedStatus=null; _invLoadedPayment=null; _invPrevCurrency='TZS'; invNumberEdited=false; const numEl=$('invNumber'); if(numEl){ numEl.value=''; numEl.removeAttribute('readonly'); } const lockEl=$('invNumLock'); if(lockEl) lockEl.style.display='none'; const issueBtn=$('invBtnIssue'); if(issueBtn){ issueBtn.disabled=false; issueBtn.style.display=''; } const voidBtn=$('invBtnVoid'); if(voidBtn) voidBtn.style.display='no`
- line 14079 · hash `9e3f6ac24964` · `function invNewInvoice(){ _invActiveId=null; _invBankManuallyChosen=false; _invLoadedStatus=null; _invLoadedPayment=null; _invPrevCurrency='TZS'; invNumberEdited=false; const numEl=$('invNumber'); if(numEl){ numEl.value=''; numEl.removeAttribute('readonly'); } const lockEl=$('invNumLock'); if(lockEl) lockEl.style.display='none'; const issueBtn=$('invBtnIssue'); if(issueBtn){ issueBtn.disabled=false; issueBtn.style.display=''; } const voidBtn=$('invBtnVoid'); if(voidBtn) voidBtn.style.display='no`
- identical normalized bodies: **yes**

## `invPaymentRecalc` — 2 declarations
- line 14885 · hash `903d69f89623` · `function invPaymentRecalc(){ if(!_invPaymentCtx) return; const {total,subtotal,currentCash,currentWht}=_invPaymentCtx; const cur=val('invCurrency')||'TZS'; const whtOn=$('invPayWhtSection')?.dataset.active==='1'; const cash=Number(val('invPayCash'))||0; let wht=0; if(whtOn){ const rate=Number(val('invPayWhtRate'))||0; wht=Math.round(subtotal*rate/100); if($('invPayWhtAmountDisplay')) $('invPayWhtAmountDisplay').value=`${cur} ${wht.toLocaleString()}`; } const newCashTotal=currentCash+cash, newWht`
- line 16433 · hash `8d1e5f91d1af` · `function invPaymentRecalc(){ if(!_invPaymentCtx) return; const isProforma=_invPaymentCtx.invoiceType==='proforma'; let total=isProforma?Number(val('invPayAgreedTotal')):Number(_invPaymentCtx.originalTotal||0); if(!Number.isFinite(total)||total<0) total=0; const vatRate=Number(_invPaymentCtx.vatRate||0); const subtotal=vatRate>0?Math.round((total/(1+vatRate/100))*100)/100:total; _invPaymentCtx.total=total; _invPaymentCtx.subtotal=subtotal; const currentCash=Number(_invPaymentCtx.currentCash||0), `
- identical normalized bodies: **no**

## `invRenderSavedList` — 2 declarations
- line 13029 · hash `7d5dd1ea1a67` · `function invRenderSavedList(){ const el=$('invSavedList'); if(!el) return; if(!_invAllInvoices.length){ el.innerHTML='<div style="padding:12px;color:var(--muted);font-size:12px;text-align:center">No invoices yet</div>'; return; } const STATUS_COLORS={ draft:'#64748b',issued:'#1e40af',paid:'#065f46', part_paid:'#92400e',overdue:'#991b1b',cancelled:'#6b7280',void:'#6b7280', }; el.innerHTML=_invAllInvoices.map(inv=>{ const col=STATUS_COLORS[inv.status]||'#64748b'; const clientDisplay=inv.client_nam`
- line 13973 · hash `7d5dd1ea1a67` · `function invRenderSavedList(){ const el=$('invSavedList'); if(!el) return; if(!_invAllInvoices.length){ el.innerHTML='<div style="padding:12px;color:var(--muted);font-size:12px;text-align:center">No invoices yet</div>'; return; } const STATUS_COLORS={ draft:'#64748b',issued:'#1e40af',paid:'#065f46', part_paid:'#92400e',overdue:'#991b1b',cancelled:'#6b7280',void:'#6b7280', }; el.innerHTML=_invAllInvoices.map(inv=>{ const col=STATUS_COLORS[inv.status]||'#64748b'; const clientDisplay=inv.client_nam`
- identical normalized bodies: **yes**

# Duplicate DOM IDs
## `adminOnly` — 2 occurrences
- line 1430 · `<div style="font-size:11px;font-weight:700;letter-spacing:.4px;color:var(--muted);margin-bottom:8px">INACTIVE MEMBERS</div> <div id="inactiveMembersList"></div> </div> </div> <!-- Add Member — genuinely different task (creation), kept separate --> <div class="card hidden" id="adminOnly"> <h2 id="memberFormTitle">Add Member</h2> <div class="notice note">Admin must set a temporary password for every new member. Share it privately with the staff member.</div> <div class="row"> <div><label>Full Name</label><input id="memName" placeholder="Staff full name"></div>`
- line 6325 · `return `<div class="item"><span class="pill ${badgeCol}">${badgeLabel}</span> <span class="pill ${active?'green':'red'}">${active?'Active':'Inactive'}</span><div class="item-title">${esc(m.name)}</div><div class="item-meta"><b>Position:</b> ${esc(m.position||'—')}<br><b>Email:</b> ${esc(m.email||'—')} · <b>Phone:</b> ${esc(m.phone||'—')}</div>${actions}</div>`; }; // Add Member card visibility (was previously misleadingly wrapping the // whole admin section under id="adminOnly" -- it now only wraps the // Add Member form, since the member list is merged into the single // Team Members card above and shows Edit/Deactivate inline per-admin). if($('adminOnly')) $('adminOnly').classList.toggle('`

## `exReceiptsPlaceholder` — 2 occurrences
- line 2252 · `<th style="padding:5px 8px;text-align:right;color:var(--muted);font-size:10px;font-weight:700;letter-spacing:.3px">AMOUNT (TZS)</th> <th style="padding:5px 4px;width:28px"></th> </tr></thead> <tbody id="exReceiptsBody"> <tr id="exReceiptsPlaceholder"><td colspan="5" style="padding:12px 8px;color:#ccc;font-size:12px;text-align:center">No receipts recorded yet</td></tr> </tbody> </table> </div> <div style="margin-top:10px;padding-top:8px;border-top:2px solid var(--border);display:flex;justify-content:space-between;align-items:center">`
- line 13296 · `// ── RECEIPTS ───────────────────────────────────────────────────────────────── function exRenderReceipts(){ const tbody=$('exReceiptsBody'); if(!tbody) return; const ph=document.getElementById('exReceiptsPlaceholder'); if(ph&&_exReceipts.length) ph.remove(); if(!_exReceipts.length){ tbody.innerHTML=`<tr id="exReceiptsPlaceholder"><td colspan="5" style="padding:14px 8px;text-align:center;color:#ccc;font-size:12px">No money received yet</td></tr>`; if($('exTotalReceived')) $('exTotalReceived').textContent='TZS 0'; return; } const total=_exReceipts.reduce((s,r)=>s+Number(r.amount||0),0); tbody.innerHTML=_exReceipts.map(r=>`<tr style="border-bottom:1px solid #f5f0e8">`

## `fdReceivablesCardV1` — 2 occurrences
- line 16538 · `window.fdCarryForwardSnapshot=function(){const d=new Date((_fdPeriodStart||new Date()).toString()),start=d.toISOString().slice(0,10),pm=(_fdAllManual||[]).filter(t=>t.date<start&&approved(t)),cin=pm.filter(t=>['revenue','loan'].includes(t.tx_type)).reduce((s,t)=>s+Number(t.amount||0),0)+(_fdAllInvoices||[]).flatMap(fdInvoiceCashReceipts).filter(r=>r.date<start).reduce((s,r)=>s+Number(r.amount||0),0),cout=pm.filter(t=>['expenditure','drawing'].includes(t.tx_type)).reduce((s,t)=>s+Number(t.amount||0),0);return {openingCash:cin-cout};}; window.fdRenderReceivablesCard=function(){let h=document.getElementById('fdReceivablesCardV1');if(!h){h=document.createElement('div');h.id='fdReceivablesCardV1'`
- line 16699 · `if(typeof go==='function')go('findash'); await waitV2(); if(typeof fdEditManual==='function')await fdEditManual(id); } } window.fdOpenReceivableSourceV2=openSourceV2; function renderReceivablesV2(){ let card=document.getElementById('fdReceivablesCardV1'); if(!card){ card=document.createElement('div');card.id='fdReceivablesCardV1';card.className='card'; const k=document.getElementById('fdKpiRow'); if(k)k.insertAdjacentElement('afterend',card); } const src=sourcesV2(), groups={}; src.forEach(r=>{const k=normV2(r.client)+'|'+r.currency;if(!groups[k])groups[k]={client:r.client,currency:r.currency,amount:0,sources:[]};groups[k].amount+=r.amount;groups[k].sources.push(r);});`

